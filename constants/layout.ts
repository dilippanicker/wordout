// Desktop web only: the game renders as a fixed-size phone-shaped card
// centered on a dark backdrop, rather than stretching full window size.
// 430x932 matches iPhone 14 Pro Max's logical resolution -- a recognizable
// "large phone" bound rather than an arbitrary number.
export const WEB_CARD_MAX_WIDTH = 430;
export const WEB_CARD_MAX_HEIGHT = 932;
